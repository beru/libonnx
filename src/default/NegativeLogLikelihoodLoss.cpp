#include "onnx.h"
#include "util.h"

namespace onnx {

namespace {
} // namespace

operator_t* resolver_default_op_NegativeLogLikelihoodLoss(int opset)
{
	return nullptr;
	//if (opset >= 13) {
	//}else if (opset >= 12) {
	//}
}

} // namespace onnx
